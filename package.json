{
  "name": "pong-game",
  "version": "1.0.0",
  "description": "Retro Neon Pong compiled to WebAssembly",
  "scripts": {
    "start": "serve public",
    "build": "docker run --rm -v $(pwd):/src -u $(id -u):$(id -g) emscripten/emsdk emcc src/main.cpp -o public/wasm/pong.js -I include -s LEGACY_GL_EMULATION=1 -s WASM=1"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "dependencies": {
    "puppeteer": "^25.2.1"
  }
}
